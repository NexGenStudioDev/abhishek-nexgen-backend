import technologyConstant from '../technology.constant';

describe('Technology constant', () => {
  test('Check if the technology constant is defined', () => {
    expect(technologyConstant).toBeDefined();
  });
});
